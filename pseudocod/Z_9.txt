Z-9 

//Сергеев

//z1 — делимое, целое
//z2 — делитель, натуральное
//g — промежуточная переменная, натуральное
//r — результат деления, целое

ФУНКЦИЯ DIV_ZZ_Z (z1, z2)
	g := DIV_NN_N (ABS_Z_N(z1), z2) // Нахождение неполного частного модуля делимого на делитель
	ЕСЛИ (POZ_Z_D (z1) = (2)) // Если делимое положительно
		r := TRANS_N_Z (g) // Преобразуем результат в целое цисло
	ИНАЧЕ // Если делимое неположительно
		ЕСЛИ (COM_NN_D (ABS_Z_N (z1), z2) != 0) // Проверяем, не равно ли оно по модулю делителю 
			g := ADD_1N_N (g) // и, если не равно, прибавляем к результату единицу
		КЕ
		r := TRANS_N_Z (g) // Преобразуем результат в целое цисло
		r := MUL_ZM_Z (r) // Умножаем результат на -1
	КЕ
	ВОЗВРАТ r		
КФ
